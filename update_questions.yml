name: Auto update history questions

on:
  schedule:
    # 02:17 UTC Monday = 09:17 Monday in Viet Nam (UTC+7)
    - cron: "17 2 * * 1"
  workflow_dispatch:
    inputs:
      count:
        description: "Number of new Vietnam-history questions"
        required: false
        default: "12"

permissions:
  contents: write

jobs:
  update-questions:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate new history questions
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          QUESTION_COUNT: ${{ inputs.count || '12' }}
        run: python scripts/generate_questions.py --count "$QUESTION_COUNT"

      - name: Validate question files
        run: python scripts/validate_data.py

      - name: Commit and push if changed
        run: |
          if git diff --quiet; then
            echo "No question changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add manifest.json history_vietnam.json
          git commit -m "Auto-update history questions"
          git push
